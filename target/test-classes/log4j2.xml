<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="INFO">
	<Properties>
		<Property name="log-path">./logs</Property>
		<Property name="log-pattern">%d{yyyy-MM-dd HH:mm:ss} %-5p %c{1}:%L - %m%n
		</Property>
	</Properties>
	<!-- Appenders: define *where* to generate logs (console or file) -->
	<Appenders>
		<Console name="Console" target="SYSTEM_OUT">
			<PatternLayout pattern="${log-pattern}" />
		</Console>

		<RollingFile name="File"
			fileName="${log-path}/automation.log"
			filePattern="${log-path}/automation-%d{MM-dd-yyyy}-%i.log.gz">
			<PatternLayout pattern="${log-pattern}" />
			<Policies>
				<TimeBasedTriggeringPolicy />
				<SizeBasedTriggeringPolicy size="1MB" />
			</Policies>
			<DefaultRolloverStrategy max="10" />
		</RollingFile>
	</Appenders>

	<!-- ===================================================== LOGGERS: define 
		WHAT TYPE (LEVEL) of logs to generate Levels hierarchy (from lowest → highest): 
		TRACE < DEBUG < INFO < WARN < ERROR < FATAL ===================================================== -->
	<Loggers>
		<!-- Root logger controls the overall logging level for the project -->

		<Root level="Debug">
			<AppenderRef ref="Console"/> 
			<AppenderRef ref="File" />
		</Root>
	</Loggers>
</Configuration>
